const esc = (v: unknown) => String(v ?? "").replace(/[&<>"']/g, (c) => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]!));
export { esc };
export function baseLayout({ preheader, title, badge, body, cta }: { preheader: string; title: string; badge?: string; body: string; cta?: { label: string; href: string }[] }) {
  const site = process.env.SITE_URL || process.env.NEXT_PUBLIC_APP_URL || "https://art-visions.fr";
  const buttons = (cta || []).map((b) => `<a href="${esc(b.href)}" style="display:inline-block;background:#FF6A00;color:#fff;text-decoration:none;font-weight:700;padding:12px 18px;border-radius:8px;margin:4px 6px 4px 0">${esc(b.label)}</a>`).join("");
  return `<!doctype html><html><body style="margin:0;background:#F8F7FC;font-family:Arial,sans-serif;color:#171625"><div style="display:none;max-height:0;overflow:hidden">${esc(preheader)}</div><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="background:#F8F7FC"><tr><td align="center" style="padding:24px 12px"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="max-width:600px;background:#fff;border-radius:16px;overflow:hidden"><tr><td style="background:#08051F;padding:28px"><img src="${site}/logo_blanc.svg" alt="Art Vision" width="150" style="display:block;max-width:150px;height:auto"><div style="height:4px;background:#6C2BD9;margin-top:20px"></div></td></tr><tr><td style="padding:30px">${badge ? `<span style="display:inline-block;background:#F0E8FF;color:#6C2BD9;font-size:12px;font-weight:700;padding:6px 10px;border-radius:99px">${esc(badge)}</span>` : ""}<h1 style="font-size:25px;line-height:1.25;margin:14px 0 20px;color:#1A1238">${esc(title)}</h1>${body}${buttons ? `<div style="margin-top:24px">${buttons}</div>` : ""}</td></tr><tr><td style="background:#1A1238;color:#F8F7FC;padding:22px 30px;font-size:12px;line-height:1.6">Art Vision — L’art au service de votre image.<br><a href="mailto:contact@art-visions.fr" style="color:#fff">contact@art-visions.fr</a> · <a href="${site}" style="color:#fff">art-visions.fr</a><br><a href="${site}/mentions-legales" style="color:#cfc7e8">Mentions légales et confidentialité</a></td></tr></table></td></tr></table></body></html>`;
}
export function rows(values: [string, unknown][]) { return values.filter(([,v]) => v !== undefined && v !== null && String(v).trim() !== "").map(([k,v]) => `<tr><td style="padding:8px 10px;border-bottom:1px solid #EDEAF5;color:#6b6478;width:34%;vertical-align:top">${esc(k)}</td><td style="padding:8px 10px;border-bottom:1px solid #EDEAF5;font-weight:600;white-space:pre-wrap">${esc(typeof v === "object" ? JSON.stringify(v, null, 2) : v)}</td></tr>`).join(""); }