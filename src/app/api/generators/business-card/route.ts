import { NextResponse } from "next/server";import { z } from "zod";import prisma from "@/lib/prisma";import { publicFormGuard } from "@/lib/email/security";import { sendLeadNotification } from "@/lib/email/lead-notification";const schema=z.object({name:z.string().min(1).max(120),jobTitle:z.string().max(160),phone:z.string().max(40),email:z.string().email().max(160),address:z.string().max(300).optional(),websiteUrl:z.string().max(500).optional(),website:z.string().optional(),socials:z.string().max(10000).optional(),templateId:z.string().max(120).optional(),colors:z.string().max(10000).optional(),qrData:z.string().max(5000).optional(),sourceUrl:z.string().url().max(500).optional()}).passthrough();export async function POST(req:Request){let b:unknown;try{b=await req.json()}catch{return NextResponse.json({success:false,error:"Requête invalide."},{status:400})}if(b&&typeof b==="object"&&"website" in b&&!((b as any).websiteUrl)){(b as any).websiteUrl=(b as any).website;(b as any).website=""}const p=schema.safeParse(b);if(!p.success)return NextResponse.json({success:false,error:"Données invalides."},{status:422});const d=p.data,g=publicFormGuard(req,"business-card",d.website);if(!g.ok)return NextResponse.json({success:false,error:g.error},{status:g.status});try{let t=await prisma.businessCardTemplate.findFirst({where:{id:d.templateId}});if(!t)t=await prisma.businessCardTemplate.create({data:{id:d.templateId||"modern-minimalist",name:d.templateId==="creative"?"Creative Gradient":"Modern Minimalist",layout:d.colors||"{}"}});const row=await prisma.generatedBusinessCard.create({data:{templateId:t.id,name:d.name,jobTitle:d.jobTitle,phone:d.phone,email:d.email.toLowerCase(),address:d.address,website:d.websiteUrl,socials:d.socials,colors:d.colors,qrData:d.qrData}});await sendLeadNotification({type:"business-card",lead:{name:d.name,email:d.email,phone:d.phone,toolName:"Générateur de carte de visite",details:{jobTitle:d.jobTitle,website:d.websiteUrl,template:d.templateId}},metadata:{sourceUrl:d.sourceUrl},relatedEntityType:"GeneratedBusinessCard",relatedEntityId:row.id});return NextResponse.json({success:true,cardId:row.id});}catch(error){console.error("Business card save failed",error instanceof Error?error.message:"unknown");return NextResponse.json({success:false,error:"Erreur lors de la sauvegarde."},{status:500})}}